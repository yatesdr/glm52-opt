# v20 production patch and PR ledger

Updated: 2026-07-25 11:35 EDT  
Published container base: vLLM `992b874cf` / SparkInfer `a93df671`  
Selected source heads: vLLM `89b4a98d1` / SparkInfer `c39b8062`

Upstream vLLM draft target:
`local-inference-lab/vllm:dev/gilded-gnosis` at `89b4a98d1`. The two new
standalone branches below are based directly on that commit, so their PR diffs
contain no image-integration carry.

## Latest-head integration pins

The production and diagnostic code are now separated:

```text
clean production:
  branch  integration/v20-prod-head-20260725
  head    625ac3b75bf26741b4d8de06a46ec803a8a80f23
  parent  89b4a98d1ffebb2dda1e1ac5e55238e3a9cfbd58

measurement-only child:
  branch  diagnostic/v20-prod-profiler-head-20260725
  head    71053e516c13279d5735a54431cb44a8111d4af3
  parent  625ac3b75bf26741b4d8de06a46ec803a8a80f23

SparkInfer:
  branch  integration/v20-current-recovery-20260724
  head    d4969d993cdd16cc417056d471af42d10ac3fada
  parent  c39b8062ba450c030e669d898a026d10980c9470
```

The clean branch carries nine separable commits: #165, Brandon Music's native
MTP3 gate plus its test, Martin Vit's #154 forward port plus documentation and
test adaptation, the staged BF16-to-FP8 guard, the clean one-commit #171
forward port, and #168. The profiler is not in the clean branch.

The published binary-to-selected-source deltas are entirely Python/Triton, so
the derived image retains the pinned stable extension. Full package manifests
cover 2,650 production vLLM files, 2,651 profiler vLLM files, and 167
SparkInfer files. See `v20-latest-head-profiler-fable-handoff.md`.

This ledger separates production-image composition from upstream review.
Each behavioral change remains independently reviewable. A consolidated image
may contain several rows, but unrelated changes must not be combined into one
new PR.

| Change | Production effect | Upstream state | Candidate | Evidence | Remaining gate |
|---|---|---|---|---|---|
| SparkInfer persistent PCIe-DMA output | Prevents late all-reduce allocation/OOM; reuses a stable graph-safe output; protects prefill | [SparkInfer #76](https://github.com/local-inference-lab/sparkinfer/pull/76), open/ready, Martin's authorship; already cherry-picked in base as `cd089a4`, stable patch-id identical to PR head `8670c57` | Already present | 52 unit tests and upstream GPU tests; CN4 four-rank Proof 2 shows default output is finite, rank-identical, graph-replay-correct, and approximately equal to explicit-output latency | Attach final CN4 JSONL SHA to #76; do not re-apply the functional commit |
| PCIe-DMA output storage validation | Capacity/tail/type/explicit-output/close correctness for #76; no separate claimed speedup | Local dependent draft `4675b3388fe2a82b2ce1c8ad9faf30123909a7cb` | Include | Static compile/diff pass; exercises the exact persistent-output contract | Prefer cherry-pick into #76; otherwise open a dependent draft after Linux tests |
| Bounded filesystem KV tier | Caps NVMe usage and evicts LRU entries safely | [vLLM #165](https://github.com/local-inference-lab/vllm/pull/165), open/ready | Include | 71 focused tests; ordered 8 GiB saturation run observed 41,334 events and stayed 823,296 bytes under cap | Consolidated 64 GB turnover/promotion and identity audit |
| Native B12X MTP3 flattening gate | Keeps native sparse MLA path for SM120/B12X MTP3 instead of an incompatible flattened path; long-context correctness | Standalone current-head branch `83782e474` + `146199ef9`; PR body prepared in `pr-v20-b12x-native-mtp3.md` | Include | Independent TP4/DCP4/MTP3 field configuration passed 9/9 cold needles through 490,658 tokens | Publish draft after consolidated cold ladder and decode |
| Release absorbed MXFP8 `kv_b_proj` source | Reclaims about 290 MiB/GPU after absorbed weights are built; increases KV capacity | [vLLM #154](https://github.com/local-inference-lab/vllm/pull/154), open/ready but currently conflicting; preserve Martin's authorship; current-head port is `f0828f326` + `6db0f6841` + test adaptation `930bb9675` | Include with `VLLM_B12X_ABSORB_BMM=0` | Upstream lifecycle tests pass; prior field result reclaimed about 290 MiB/GPU with unchanged quality/performance | Give the conflict-resolved commits to Martin; record exact pool delta and complete long-context ladder |
| BF16-weight to FP8-query correction | Preserves the byte-defining safe BMM/assembly/static-quantization route for BF16-weight/FP8-output while retaining BF16-output and MXFP8 fusion | Standalone current-head commit `3be735af3`; PR body prepared in `pr-v20-staged-bf16-fp8-query.md` | Include | GPU proof: top-k 160/160 exact; fused query differed from staged in 17/60 cases, while fused-BF16+static and direct-FP8 were byte-identical, localizing divergence to projection order; artifact `eb8b4e49…`; source proof shows production-sized prefill is outside the tiny-M route | Publish draft after consolidated cold ladder; matched prefill/decode quantify end-to-end cost |
| Compact NVFP4 MTP verifier qualification | Keeps `nvfp4_ds_mla` on established extend verifier in `auto`; removes at least 99.38 MiB/GPU of decode-verifier scratch, but may give up the newer decode-verifier speed path | [vLLM #171](https://github.com/local-inference-lab/vllm/pull/171), open/draft with polluted three-commit head; clean one-commit current-head replacement `eaa79498` prepared locally | Include | Old pre-`992/a93` v20 passed/finalized 250k, then genuinely selected the wrong ticket at 350k and missed/finalized empty at 475k under an 8,000-token budget; exact top-k proof passed 160/160, leaving a residual verifier defect beyond the separate new query regression | After Fable's gate, replace the existing draft branch rather than opening a duplicate; then attach cold ladder, decode/MTP, and pool delta |
| MRV2 global graph-pool reuse | Makes gross-minus-retained accounting reusable by production capture; recovered 11,520 KV tokens in isolated field run | [vLLM #168](https://github.com/local-inference-lab/vllm/pull/168), open/ready | Include | Existing unit test plus isolated 544,000 to 555,520-token result; did not mask the separate query-BMM crash | Prove current-base capture and preserve capacity gain |
| Exact long-context top-k correction | Only if the exact DCP4 two-level microproof fails | No PR unless proven | Conditional | Final Proof 3 covers 32,767/32,768/32,769 and observed DCP4 widths | Do not patch or open a PR if the proof passes |
| MRV2 persistent-resource startup profiling | Profiles activation peaks on top of persistent kernel resources and avoids duplicate synthetic workspace during graph capture | [vLLM #172](https://github.com/local-inference-lab/vllm/pull/172); functional patch already present in base as `cb27f671d` | Already present | Source audit confirms the base contains the warmup-once/two-pass profile and graph-capture workspace gate | Do not overlay or open another PR; #168 is complementary graph-pool lifetime accounting |
| Sparse-indexer query split | Divides indexer query rows and gathers only final int32 indices | [vLLM #175](https://github.com/local-inference-lab/vllm/pull/175), open/ready | Exclude for TP4/DCP4 | At TP4/DCP4 each transposed query-split group has world size 1, so the feature has no work to divide; measured gains apply to TP8 and DCP1-class groups | Keep `VLLM_DCP_QUERY_SPLIT=0`; reconsider only for another topology |
| Capacity-pitched DCP workspace | Existing concurrency/context allocation correction | vLLM #167, merged and already in the base | Already present | Runtime/source audit complete | No new PR |

## Draft publication rule

Publish or update drafts only after the proof associated with that row is
attached. Do not create duplicates for #154, #165, #168, #171, or #76.

The two new vLLM drafts currently expected are:

1. native B12X MTP3 flattening gate;
2. staged BF16-weight/FP8-query route, selected by Proof 3.

The SparkInfer validation work should first be offered as a child commit to
Martin's #76. The base already carries #76 itself; a separate dependent draft
for the validation delta is the fallback, not the default.

PR #171 needs a reviewed force-update of its existing head from the polluted
three-commit history to clean commit `eaa79498`; do not create a duplicate.
PR #165's public description still says the runtime gate is pending even
though the ordered 41,334-event test observed zero violations and a high-water
823,296 bytes under the cap. Update that evidence before final review.

## Prebuilt integration alternatives

Both exact-base branches already include #165, the native MTP3 gate, #154,
the staged query guard, and #168:

| Branch HEAD | Verifier posture | Use when |
|---|---|---|
| `e16288f5d` | includes #171 compact-NVFP4 extend qualification | selected: old v20 passes 250k but genuinely fails at 350k |
| `b8534c4a5` | retains current compact-NVFP4 decode verifier | retained only as the measured route A/B; not the production candidate after the 350k failure |

The final build pin selects one; neither branch implies a second model boot.
